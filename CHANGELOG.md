# Changelog

## [1.0.0] — 2025-07-09

Initial release.

### Tools

- `routes_list` — List all routes with method, URI, controller, middleware
- `routes_cache` — Cache routes for faster registration
- `routes_clear` — Clear route cache
- `models_list` — List all Eloquent models
- `model_detail` — Show model columns, relations, casts, policies
- `controllers_list` — List controllers with optional route mapping
- `migrations_list` — Migration status
- `schedule_list` — Scheduled tasks
- `events_list` — Events and listeners
- `middleware_list` — Middleware aliases
- `app_about` — Laravel version, environment, packages
- `commands_list` — All Artisan commands
- `artisan_run` — Run read-only Artisan commands

### Resources

- `laravel://about` — Application info
- `laravel://routes` — Route table
